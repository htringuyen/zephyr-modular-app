# Currently this module has no code packages. 

# If there is any package added in future, the configuraiton may look like:
# add_subdirectory_ifdef(CONFIG_${MODULE_NAME}_${PACKAGE_NAME} package_dir)

